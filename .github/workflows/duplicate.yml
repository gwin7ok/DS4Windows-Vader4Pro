name: DISABLED - duplicate issue handler

# Disabled: duplicate issue workflow replaced by consolidated maintenance.
on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "duplicate.yml disabled; see .github/workflows/release.yml"