name: DISABLED - out-of-scope issue handler

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "out-of-scope.yml disabled; see .github/workflows/release.yml"