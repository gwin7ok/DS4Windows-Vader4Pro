name: DISABLED - Auto Release (kept for history)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is disabled. Use .github/workflows/release.yml"
