<Window x:Class="DS4WinWPF.DS4Forms.ProfileNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DS4Windows Profile Notification" 
        Width="525" Height="120"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    
    <Window.Resources>
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="1" To="0" Duration="0:0:0.5"/>
        </Storyboard>
    </Window.Resources>

    <Border Background="#2D2D30" BorderBrush="#007ACC" BorderThickness="2" CornerRadius="8">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- タイトル -->
            <TextBlock Grid.Row="0" 
                       Text="DS4Windows Profile Change"
                       FontSize="16" 
                       FontWeight="Bold"
                       Foreground="#FFFFFF"
                       Margin="0,0,0,8"/>
            
            <!-- メッセージ -->
            <TextBlock x:Name="MessageTextBlock"
                       Grid.Row="1"
                       Text=""
                       FontSize="14"
                       Foreground="#E0E0E0"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"/>
        </Grid>
    </Border>
</Window>