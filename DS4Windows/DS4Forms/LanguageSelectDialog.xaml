<Window x:Class="DS4WinWPF.DS4Forms.LanguageSelectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DS4WinWPF.DS4Forms"
        mc:Ignorable="d"
        Title="Select Language" Height="500" Width="600" MinWidth="500" MinHeight="400"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Style="{DynamicResource WindowStyle}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock TextWrapping="Wrap" FontSize="16" Margin="0,0,0,10">
                <Run Text="Welcome to DS4Windows!" FontWeight="Bold"/>
            </TextBlock>
            <TextBlock TextWrapping="Wrap" FontSize="14">
                <Run Text="Please select your preferred language."/>
            </TextBlock>
        </StackPanel>

        <Border Grid.Row="1" BorderThickness="1" BorderBrush="{DynamicResource SystemControlForegroundBaseMediumBrush}">
            <ListBox x:Name="languageListBox" SelectionMode="Single" Padding="5"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseDoubleClick="LanguageListBox_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <TextBlock Text="{Binding DisplayName}" FontSize="14" FontWeight="SemiBold" Width="150"/>
                            <TextBlock Text="{Binding NativeName}" FontSize="14" Margin="20,0,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="okBtn" Content="OK" Width="100" Height="30" Margin="0,0,10,0"
                    Click="OkBtn_Click" IsDefault="True"/>
            <Button x:Name="cancelBtn" Content="Cancel" Width="100" Height="30"
                    Click="CancelBtn_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
