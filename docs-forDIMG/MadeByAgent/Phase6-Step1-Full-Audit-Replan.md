# Phase6-Step1 完全監査再計画（制約解除版）

作成日: 2026-09-18
固定スナップショット（変更後）: `aa0e503c1b6f241dd14d0f01224d211f2a20f008`
前固定スナップショット: `62e69387ebb99d32003897b261ca2ac9fb3bebd8`
対象ブランチ: `For-DI-migration-work`

## 制約解除の記録
- ビルド許可: `dotnet build` を監査の一部として使用可（意味解析の完全化のため）。
- 実機検証許可: 実機での入力・出力動作確認を完了判定に含める。
- 作業ツリー編集許可: 監査結果に基づく修正（分類確定、訂正、残課題の反映）をファイル編集で行う。

## 固定スナップショット変更の理由
- 前固定 `62e69387…` は監査ツール生成と照合結果の記録を含むコミットであり、監査の対象ソースとしては適切だった。
- 現在の最新 `aa0e503c1b6f241dd14d0f01224d211f2a20f008` は、その記録を含む最新状態であり、完全監査の基準としてより適切（監査結果と対象ソースが同一コミットに含まれる）。
- `DS4Windows/` ツリーの内容は前固定と同一（監査ツールと文書の追加のみで本番コード変更なし）。

## 完全監査の追加要件（制約解除により実行可能になったもの）
1. `dotnet build` による完全コンパイルとRoslyn意味解析（19,899件のコンパイルエラー解消を含む）。
2. `ScpUtil.cs` の全静的メンバ（503件抽出済み）の完全台帳と、全参照式（2,142件抽出済み）の一意ID照合。
3. サービス別紙（`Phase6-Step1-Service-Reference-Evidence.md`、未検証メモ）の個別参照への展開と分類確定。
4. `Phase5-Step14`（自動テスト・実機検証）と `Step15`（Legacy shim削除判断）の完了証跡確認。
5. 分類別実測集計（a/b/c別、除外理由別、ホットパス別）の生成と、上位計画の193件暫定値との差分検証。
6. 実機での入力・出力動作確認（ホットパス性能維持、スロット再接続連動を含む）。

## 現在の状態（変更なし、再確認）
- 固定スナップショット照合完了（上記範囲）。
- 監査ツール生成と抽出結果記録完了（503メンバ、2,142参照候補、188未束縛、190非本番、19,899コンパイルエラー、生成台帳7ファイル）。
- 全件分類監査（193件のa/b/c確定、全参照式網羅性証明、分類別実測集計、Phase5完了証跡確認）は**引き続き未完了**。
- 本番コード変更なし。追加作業は監査文書とツールのみ。
